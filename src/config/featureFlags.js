const featureFlags = {
  DESIGN_STUDIO_ENABLED: true,
  MQTT_ENABLED: true,
  REPORTS_ENABLED: true,
  DARK_MODE_ENABLED: true,
  NOTIFICATIONS_ENABLED: true,
  DS_PROJECT_LINK_ENABLED: true, // Cross-domain: WE.OKAS project → Design Studio
};

export default featureFlags;
